--extra-index-url https://pypi.aikido.io/1d4ac21a07db056171635
requests
beautifulsoup4
lxml
trafilatura
markdownify
ddgs
playwright

# parse.py (document parsing) dependencies:
pymupdf
python-docx
pandas
openpyxl
python-pptx
tabulate

# optional, only needed for `parse.py --ocr` on scanned/image PDFs:
# pytesseract
# pillow
# (plus the tesseract binary itself, e.g. `brew install tesseract` on macOS)
h11==0.14.0+aikido.2